/* autogenerated by Processing revision 1292 on 2023-03-07 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class Fractal extends PApplet {

int WindowSizeX = 1920;
int WindowSizeY = 1080;
int BgColor = color(255, 255, 255);

float _strutFactor = 0.05f;

class Root{
  PVector[] Polygon;
  int Color;
  Branch _Branch;

  Root(int n_points, float radius, int _color){
    Color = _color;
    Polygon = new PVector[n_points];

    float angle = TWO_PI / n_points;

    for (int i = 0; i < n_points; i += 1) {
      float a = angle * i;
      float sx = cos(a) * radius;
      float sy = sin(a) * radius;
      Polygon[i] = new PVector(sx, sy);
    }

    _Branch = new Branch(0, 0, Polygon, Color);
  }

  public void display(){
    pushMatrix();
    translate(WindowSizeX/2, WindowSizeY/2);
    beginShape();
    for (int i = 0; i < Polygon.length; i += 1) {
      stroke(Color);
      strokeWeight(2);
      vertex(Polygon[i].x, Polygon[i].y);
    }
    endShape(CLOSE);
    popMatrix();
  }

}

class Branch{
  int level;
  int n;
  PVector[] Polygon;
  int Color;

  Branch(int _level, int _n, PVector[] _Polygon, int _color){
    level = _level;
    n = _n;
    Polygon = strut_points(mid_points(_Polygon));
    Color = _color;
  }

  public void display(){
    pushMatrix();
    translate(WindowSizeX/2, WindowSizeY/2);
    beginShape();
    for (int i = 0; i < Polygon.length; i += 1) {
      stroke(Color);
      strokeWeight(2);
      vertex(Polygon[i].x, Polygon[i].y);
    }
    endShape(CLOSE);
    popMatrix();
  }

  public PVector[] mid_points(PVector[] _Polygon){
    PVector[] MidPoints = new PVector[_Polygon.length];

    int next_i;
    for (int i = 0; i < _Polygon.length; ++i) {
      if (i == _Polygon.length - 1){
        next_i = 0;
      } else {
        next_i = i + 1;
      }
      MidPoints[i] = PVector.mult(PVector.add(_Polygon[i], _Polygon[next_i]), 0.5f); 
    }

    return MidPoints;
  }

  public PVector[] strut_points(PVector[] _Polygon){
    PVector[] StrutPoints = new PVector[_Polygon.length];

    int next_i;
    for (int i = 0; i < _Polygon.length; ++i) {
      next_i = i + 3;
      if (next_i >= _Polygon.length) { 
        next_i -= _Polygon.length; 
      }
      StrutPoints[i] = strut_2points(_Polygon[i], _Polygon[next_i]);
    }

    return StrutPoints;
  }

  public PVector strut_2points(PVector _Point1, PVector _Point2){
    float px, py;
    float adj, opp;

    opp = abs(_Point2.x - _Point1.x);
    adj = abs(_Point2.y - _Point1.y);

    px = _Point1.x - Math.signum((int) (_Point1.x - _Point2.x)) * (opp * _strutFactor);
    py = _Point1.y - Math.signum((int) (_Point1.y - _Point2.y)) *(adj * _strutFactor);

    return new PVector(px, py);

  }

}

public void settings() {
  size(WindowSizeX, WindowSizeY);
}

public void setup() {
  frameRate(60);
  noFill();
}

public void draw() {
  background(BgColor);
  Root Root0 = new Root(5, 400, color(0, 0, 0));
  Root0.display();
  Root0._Branch.display();
  
}


  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "Fractal" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
